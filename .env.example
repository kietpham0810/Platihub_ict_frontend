# Sao chép file này thành .env (local) hoặc .env.production và điền giá trị thật.
VITE_API_BASE_URL=http://localhost:8080/api
VITE_TIMEOUT_MS=5000

# Tài khoản đăng nhập khu vực quản trị (tạm thời phía frontend - xem ghi chú bảo mật trong bug.txt)
VITE_ADMIN_USERNAME=platihubict
VITE_ADMIN_PASSWORD=changeme
